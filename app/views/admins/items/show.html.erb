<h1>商品詳細</h1>
<table>
  <tr>
    <td><%= @item.name %></td>
    <td><%= @item.item_spec %></td>
    <td><%= @item.price %></td>
    <td><%= @item.genre.name %></td>
  </tr>
</table>
<%= link_to '編集する', edit_admins_item_path(@item.id) %>