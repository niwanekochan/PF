<h1>ジャンル編集</h1>
<%= form_with model: @genre, url:admins_genres_path, local:true do |f| %>
<table>
  <tr>
    <th>ジャンル名</th>
    <td><%= f.text_field :name %></td>
    <td><%= link_to '変更を保存', admin_genres_path %></td>
  </tr>
</table>
<% end %>